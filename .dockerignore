node_modules
.next
.git
# Every env file. Next copies .env and .env.production into the standalone
# output, so one that reaches the build context ships in the public image.
.env*
design-reference
# Deployment files for one server. They can hold its passwords.
infra
e2e
test-results
playwright-report
*.tsbuildinfo
website
